# Initial vault population proposal

Status: Proposed.

This proposal preserves documentation that existed before the top-down vault authority spine was established. Its location does not make its claims canonical.

## Preserved material

- [`architecture/`](./architecture/) contains the architecture explanation and interactive system map.
- [`contracts/`](./contracts/) contains the MCP and extension relay contract documents.
- [`operations/`](./operations/) contains the security model and real-world test runbook.

The agent-facing portions of the MCP contract are candidate inputs for `03-User-Interface/`; its internal binding and routing claims remain candidate System evidence. Architecture, relay-protocol, and operational material are candidate inputs for `04-System/` after the User Interface contract is explicitly confirmed. Promotion requires reviewing every claim against its owning higher-level contract and executable evidence.

Parent: [`90-Proposals`](../_MOC.md).
